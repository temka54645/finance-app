# Local-only Postgres for development.
# Production deploy uses Railway's managed Postgres service.

services:
  postgres:
    image: postgres:16-alpine
    container_name: finance-app-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: finance
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d finance"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
